Next.js-এ State বলতে আমরা মূলত React State-কেই বুঝি। State ব্যবহার করা হয় কোনো কম্পোনেন্টের ডাটা ধরে রাখা, UI আপডেট করা এবং ইউজারের interaction অনুযায়ী পরিবর্তন দেখানোর জন্য।

আমি ধাপে ধাপে, সহজ ভাষায় ব্যাখ্যা করছি 👇

🔹 State কী?

State হলো এমন ডাটা যা:

কম্পোনেন্টের ভিতরে থাকে

সময়ের সাথে পরিবর্তন হতে পারে

State পরিবর্তন হলে UI আবার render হয়

উদাহরণ:

বাটনে ক্লিক করলে সংখ্যা বাড়ানো

ফর্মে ইনপুট ভ্যালু রাখা

API থেকে ডাটা এনে দেখানো

🔹 Next.js-এ State কোথায় ব্যবহার করা যায়?

Next.js-এ দুই ধরনের কম্পোনেন্ট আছে:

1️⃣ Server Component (ডিফল্ট)

এখানে useState ব্যবহার করা যায় না

শুধু ডাটা fetch ও render করার জন্য

2️⃣ Client Component

এখানে useState ব্যবহার করা যায়

interactive কাজের জন্য

👉 Client Component বানাতে ফাইলের উপরে লিখতে হয়:

"use client";

🔹 Basic State উদাহরণ (Counter)
"use client";

import { useState } from "react";

export default function Counter() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <h1>Count: {count}</h1>
      <button onClick={() => setCount(count + 1)}>
        Increase
      </button>
    </div>
  );
}

এখানে কী হচ্ছে?

count → বর্তমান state

setCount → state পরিবর্তন করার function

useState(0) → শুরুতে count = 0

🔹 Form Input State উদাহরণ
"use client";

import { useState } from "react";

export default function Form() {
  const [name, setName] = useState("");

  return (
    <div>
      <input
        type="text"
        placeholder="Your name"
        onChange={(e) => setName(e.target.value)}
      />
      <p>আপনার নাম: {name}</p>
    </div>
  );
}


👉 ইউজার টাইপ করলেই UI আপডেট হচ্ছে — এটিই State এর কাজ।

🔹 Object State উদাহরণ
const [user, setUser] = useState({
  name: "",
  email: ""
});


Update করার সময়:

setUser({
  ...user,
  name: "Shakil"
});

🔹 Array State উদাহরণ (List)
const [items, setItems] = useState([]);

setItems([...items, "New Item"]);

🔹 Next.js-এ State কেন গুরুত্বপূর্ণ?

✔ UI interactive করতে
✔ Form handling
✔ Modal / Dropdown open-close
✔ API data store করতে
✔ Dashboard, Admin Panel বানাতে

🔹 মনে রাখার বিষয় ⭐

❌ Server Component-এ state নেই

✅ Client Component-এ state আছে

❌ Direct state পরিবর্তন করা যাবে না

✅ সবসময় setter function ব্যবহার করতে হবে

🔹 বাস্তব উদাহরণ

আপনি যদি বানান:

Login page → email, password state

Cart system → cart items state

Admin panel → table data state

সব জায়গায় State দরকার হবে।
