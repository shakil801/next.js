.slice() শুধু array-এর জন্য valid।
Next.js-এ slice() আসলে JavaScript-এরই একটি Array / String method। Next.js আলাদা করে কিছু যোগ করে না—কারণ Next.js পুরোপুরি JavaScript/React-এর ওপর চলে।

🔹 slice() কী? (সহজ ভাষায়)

slice() দিয়ে আমরা
👉 array বা string-এর নির্দিষ্ট অংশ কেটে নিতে পারি
👉 মূল array/string পরিবর্তন হয় না

🔹 Array-এ slice() (সবচেয়ে বেশি ব্যবহার হয়)
🔸 Syntax
array.slice(startIndex, endIndex)


startIndex → যেখান থেকে শুরু হবে

endIndex → এর আগ পর্যন্ত (এটা include হয় না)

🔸 উদাহরণ (Next.js / React-এ)
const categories = ["Tech", "Education", "Health", "Sports", "Travel"];

const limitedCategories = categories.slice(0, 3);

console.log(limitedCategories);


📌 Output:

["Tech", "Education", "Health"]


👉 মানে: প্রথম ৩টা item নেওয়া হয়েছে

🔹 JSX-এর ভেতরে ব্যবহার (Next.js component)
{categories.slice(0, 4).map((cat, index) => (
  <li key={index}>{cat}</li>
))}


👉 এখানে শুধু প্রথম ৪টা category দেখাবে

🔹 আপনার আগের Error কেন হচ্ছিল?

আপনি লিখেছিলেন:

categories?.slice(0, 10)


Error:

categories?.slice is not a function

❌ কারণ:

categories array না, হয়তো:

object

null

API থেকে ভুল format আসছে

✅ সমাধান:
const limitedCategories = Array.isArray(categories)
  ? categories.slice(0, 8)
  : [];

🔹 String-এ slice() (অল্প ব্যবহার)
const text = "Next.js শেখা সহজ";
console.log(text.slice(0, 6));


📌 Output:

Next.j

🔹 মনে রাখবেন

✅ slice() মূল data পরিবর্তন করে না
✅ নতুন array/string রিটার্ন করে
✅ pagination, limit, UI show-hide-এ খুব কাজে লাগে
